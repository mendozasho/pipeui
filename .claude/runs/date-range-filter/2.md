# Slice 2 — date_range builtin: registration, validator, executor, catalog
Issue: #117 (sub-issues #120, #121, #122)   Branch: feature/date-range-filter-2

## Plan (written before coding)
- Approach per layer (data / business / api / ui):
  - **data (#120):** add the `date_range` row to `SEED_BUILTINS` in
    `src/pipeui/backend/data/base/schema/queries.py` (builtin_id `...0005`,
    config_schema `{"groups": "array"}`). Pre-feature-DB migration follows the
    rename (#40) precedent: `SEED_BUILTINS` is `INSERT OR IGNORE` and re-runs on
    every `create_schema` call (app startup — `middleware/deps.py`), so an existing
    DB gains the row on next start and re-running is idempotent (UNIQUE on
    `builtin_type`). No `_REGISTRY_SCHEMA_MIGRATIONS` entry needed — no column
    change. Proven by test, not assumed.
  - **business (#120):** register `date_range` in `BUILTIN_EXECUTORS` as
    `BuiltinSpec(validate=_validate_date_range_config, execute=<adapter lambda>,
    singleton=True, pinned_tail=1)` — slice 1's pinned-tail mechanism places it
    after all positional steps and before rename (rank 2) at all three ordering
    consumers with zero sort-site changes.
  - **business (#121):** pure validator `_validate_date_range_config(cfg) -> str | None`
    over `{"groups": [{"conditions": [{"column", "start", "end"}]}]}`. Rejects:
    zero groups, an empty group (no conditions), a condition missing a column,
    both bounds empty (None/""), start > end. Accepts one-sided conditions and
    multi-group configs. No DB access.
  - **business (#122):** executor `_execute_date_range(conn, df, cfg)` following
    `_execute_filter`'s temp-view + WHERE + bound-params pattern. One WHERE of
    OR-ed group predicates; each group an AND of inclusive range predicates
    `CAST("col" AS DATE) >= CAST(? AS DATE)` / `<= CAST(? AS DATE)` (only the
    present bounds). TIMESTAMP/TIMESTAMPTZ therefore compare at DATE granularity;
    NULL dates fail their condition by SQL semantics but the row can pass via
    another OR group.
  - api / ui: none in this slice (attach-boundary eligibility is slice 3; UI is slice 4).
- Utilities/patterns reused (file + symbol):
  - `BuiltinSpec`, `BUILTIN_EXECUTORS`, `pinned_tail_rank` — builtins.py (slice 1 / #50).
  - `_execute_filter`'s temp-view/params execution shape and `_validate_filter_config`'s
    error-string style — builtins.py.
  - `SEED_BUILTINS` INSERT OR IGNORE idempotent seed — queries.py (rename #40 precedent).
  - Test helpers `_make_source`, `_make_instance_table`, `_builtin_step`,
    `_builtin_order_at_all_three_consumers` — tests/test_builtins.py.
- Test command (for red-green cycles):
  `PYTHONPATH=src <venv python> -m pytest tests/test_builtins.py -q`
- planned_touched_files: [
    "src/pipeui/backend/domain/functions/builtins.py",
    "src/pipeui/backend/data/base/schema/queries.py",
    "tests/test_builtins.py",
    ".claude/runs/date-range-filter/2.md"
  ]
- Expected collateral test updates (issue-sanctioned, not scope creep): existing
  seed-count assertions in tests/test_builtins.py grow 4 -> 5 (B11, B12, B13, OCP
  registry set).

## Actual (written before push)
- What was built (1-3 sentences): The date_range builtin now exists end-to-end at
  the domain seam: a `BuiltinSpec` registered in `BUILTIN_EXECUTORS` (singleton,
  `pinned_tail=1` before rename's 2), a seeded `builtin_registry` catalog row that
  also migrates onto pre-feature DBs via the idempotent INSERT OR IGNORE seed
  `create_schema` re-runs at startup, a pure structural validator over the frozen
  `{groups:[{conditions:[{column,start,end}]}]}` config, and a DuckDB executor
  building one WHERE of OR-ed groups of AND-ed inclusive range predicates with
  columns CAST to DATE and bounds bound as parameters.
- actual_touched_files: [
    "src/pipeui/backend/domain/functions/builtins.py",
    "src/pipeui/backend/data/base/schema/queries.py",
    "tests/test_builtins.py",
    ".claude/runs/date-range-filter/2.md"
  ]
- Deviations from plan (and why): none in file set or approach. Two notes:
  (1) no `db.py` change was needed — the pre-feature-DB migration is the existing
  idempotent seed mechanism (rename #40 precedent), proven by test rather than a
  new migration entry. (2) Criteria whose enabling code landed as part of an
  earlier criterion's minimal green (pinned_tail=1 with registration; the column
  CAST with criterion 3; SQL OR/AND semantics) had their reds captured by
  temporarily removing exactly the behavior under test (unpin date_range; strip
  the CAST; flip OR->AND and AND->OR), confirming each test fails for the
  expected reason before restoring.
- Unanticipated files (in actual but not planned or issue touched_files): [ ]

## Acceptance (one entry per criterion — red-green evidence)
- [1: BUILTIN_EXECUTORS + catalog row fresh/migrated, idempotent, count +1] ->
  test: tests/test_builtins.py :: test_builtin_registry_seeded_with_builtin_rows,
  test_create_schema_idempotent_no_duplicates,
  test_date_range_seed_migrates_pre_feature_db_idempotently,
  test_get_builtins_endpoint, test_builtin_dispatch_is_registry_driven |
  red: "AssertionError: assert {'filter', 'j...ot', 'rename'} == {'date_range'...} —
  Extra items in the right set: 'date_range'" (5 failed) |
  green: "5 passed, 32 deselected"
- [2: validator rejects zero groups / empty group / missing column / both bounds
  empty / start > end; accepts one-sided + multi-group] ->
  test: tests/test_builtins.py :: test_validate_date_range_config_rejects_invalid_shapes,
  test_validate_date_range_config_accepts_one_sided_and_multi_group |
  red: "assert None is not None — where None = _validate_date_range_config({})"
  (placeholder accepted everything) | green: "2 passed, 38 deselected"
- [3: inclusive bounds both ends; start-only = on-or-after; end-only = on-or-before] ->
  test: tests/test_builtins.py :: test_execute_date_range_inclusive_and_one_sided_bounds |
  red: "assert [1, 2, 3, 4, 5, 6] == [2, 3, 4]" (stub executor returned every row) |
  green: "1 passed, 40 deselected"
- [4: TIMESTAMP/TIMESTAMPTZ compare at DATE granularity; 23:59 on end day kept] ->
  test: tests/test_builtins.py :: test_execute_date_range_timestamp_compares_at_date_granularity |
  red (CAST removed): "At index 0 diff: 2 != 1 — Right contains one more item: 2"
  (the 23:59 row was dropped without the DATE cast) | green: "1 passed, 41 deselected"
- [5: NULL date fails its condition; row can still pass via another OR group] ->
  test: tests/test_builtins.py :: test_execute_date_range_null_fails_condition_but_row_can_pass_via_or_group |
  red (groups joined with AND): AssertionError at the kept-ids assert (NULL-eff row
  no longer passed via the other group) | green: "1 passed, 42 deselected"
- [6: AND within a group, OR across groups; same column in two groups] ->
  test: tests/test_builtins.py :: test_execute_date_range_and_within_group_or_across_groups |
  red (conditions joined with OR): AssertionError at the kept-ids assert (rows 2/4
  wrongly kept under OR-within-group) | green: "1 passed, 43 deselected"
- [7: date_range sorts in the pinned tail after positional steps, before rename,
  at all three ordering consumers] ->
  test: tests/test_builtins.py :: test_date_range_sorts_in_pinned_tail_before_rename |
  red (pinned_tail removed): "AssertionError: ['date_range', 'filter', 'rename'] —
  assert ... == ['filter', 'date_range', 'rename']" (position order without the pin) |
  green: "6 passed, 32 deselected" (with the existing pinned-tail suite)

Final full authored-suite run: tests/test_builtins.py — 44 passed.
Full repo suite (with the known macOS-only worker OOM test deselected):
636 passed, 1 deselected.

## Follow-ups / debt
- Test evidence default: the authored tests assert stable behavioral guarantees
  (registry membership, seed migration, validator matrix, executor semantics,
  pinned-tail order), not phrasing — recommend PROMOTE into the permanent
  tests/test_builtins.py suite (they extend the existing builtin
  behavioral-guarantee file rather than a throwaway).
- Slice 3 owns date-typed column eligibility at the attach/patch write boundary
  and the persisted-config round-trip; slice 4 owns the modal UI.
- The TIMESTAMPTZ boundary test pins the DuckDB session timezone to UTC for
  determinism; product semantics remain "session-default timezone" per the PRD.
