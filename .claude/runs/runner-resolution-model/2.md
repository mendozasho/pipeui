# Slice 2 — Join honors use_transformed
Issue: #12 (sub-issues #16 business_logic, #17 api, #18 ui)   Branch: feature/runner-resolution-model-2

## Plan (written before coding)

### Approach per layer

**business_logic (#16, acceptance 0,1,2,5)** — `src/pipeui/workflow/builtins.py`
- `_execute_join` currently hardcodes `instance_table_name(right_source_id)` (raw). Route the
  right-source read through `resolve_frame(conn, right_source_id, mode)` where
  `mode = TRANSFORMED if cfg.get("use_transformed") else RAW`.
- `resolve_frame` returns `(frame, ref)`. For RAW the frame is the instance table contents; for
  TRANSFORMED it's the latest/materialized staging frame (run-once-if-absent, cycle-guarded).
  Register the returned frame as a temp view (same pattern as the existing left_df view) and join
  the left view against the right view — so both raw and transformed paths use a uniform
  "frame -> temp view -> SQL join" shape. This keeps `_execute_join` agnostic to *where* the right
  frame came from, which is the whole point of the seam.
- `_validate_join_config` only validates config shape today (no table read); it stays shape-only.
  The acceptance for it is the resolve routing, which lives in `_execute_join`. No behavior change
  needed in the validator beyond confirming it does not block use_transformed configs.
- use_transformed=false path must be byte-for-byte the prior behavior (acceptance 0).

**api (#17, acceptance 3)** — right-column-fetch endpoint
- The join modal fetches right-source columns. Add an endpoint that returns the *transformed*
  column set when a transformed flag is set, raw columns otherwise. Put it on the sources router
  as `GET /sources/{source_id}/join-columns?transformed=true|false` (sources.py owns source-column
  reads; raw path reuses the existing column_registry query, transformed path reads the resolved
  transformed frame's columns via resolve_frame).
- raw -> column_registry columns (column_id/column_name/column_type), matching `/sources/{id}`.columns.
- transformed -> columns of the resolved transformed frame (names only; type best-effort from dtype).

**ui (#18, acceptance 4)** — `src/pipeui/frontend/screen-builder.jsx` (regression + wiring)
- JoinModal already sends `use_transformed` and calls `fetchRightColumns(rid, ut)`. The wiring fn
  `fetchJoinRightColumns(rightSourceId)` ignores the 2nd arg and always hits raw `/sources/{id}`.
  Fix it to accept `(rightSourceId, useTransformed)` and call the new join-columns endpoint with the
  flag. Frontend tests assert the regression: modal sends use_transformed=true and requests the
  transformed column set when the toggle is on.

### Utilities/patterns reused (file + symbol)
- `pipeui.workflow.resolve.resolve_frame`, `RAW`, `TRANSFORMED` (slice 1) — right-source resolution.
- `_make_instance_table` / `_make_source` test helpers in tests/test_builtins.py.
- temp-view-from-df pattern already in `_execute_join` for left_df.
- `client` TestClient fixture + `get_conn` override in tests/test_api_pipelines.py.
- JoinModal vitest fixtures (renderJoin, RIGHT_COLUMNS) in screen-builder.test.jsx.

### Test command (for red-green cycles)
- python: `PYTHONPATH=src .venv/bin/pytest <path> -q`
- frontend: `npx vitest run src/pipeui/frontend/screen-builder.test.jsx`

### planned_touched_files
- src/pipeui/workflow/builtins.py
- src/pipeui/api/sources.py
- src/pipeui/frontend/screen-builder.jsx
- src/pipeui/frontend/screen-builder.test.jsx
- tests/test_builtins.py
- tests/test_api_pipelines.py
- .claude/runs/runner-resolution-model/2.md

(Note: slice ledger also lists src/pipeui/api/pipelines.py as a touched file; will only modify it
if the endpoint lands there instead of sources.py. Plan is sources.py.)

## Actual (written before push)
- What was built: `_execute_join` now resolves the right-hand source through
  `resolve_frame(conn, right_source_id, RAW|TRANSFORMED)` per the `use_transformed`
  flag and joins both sides as temp views (raw = instance table, transformed = the
  source's resolved transformed output, materialized on demand). A new
  `GET /sources/{id}/join-columns?transformed=` endpoint returns the raw registered
  columns by default and the transformed column set (from the resolved frame) when
  the flag is set. The frontend wiring fn `fetchJoinRightColumns` was fixed to pass
  the toggle through to that endpoint (it previously ignored it and always hit raw).
- actual_touched_files:
  - src/pipeui/workflow/builtins.py
  - src/pipeui/api/sources.py
  - src/pipeui/frontend/screen-builder.jsx
  - src/pipeui/frontend/screen-builder.test.jsx
  - tests/test_builtins.py
  - tests/test_api_pipelines.py
  - .claude/runs/runner-resolution-model/2.md
- Deviations from plan (and why): endpoint landed in sources.py only (not
  pipelines.py) — the join modal's column read is a source-column concern and
  sources.py already owns the column_registry queries; pipelines.py needed no change.
  `_validate_join_config` left shape-only (it never read a table; the resolve routing
  lives in `_execute_join`, which is where the use_transformed ACs are observable).
- Unanticipated files: none — actual_touched_files is a subset of the slice ledger's
  predicted touched_files (pipelines.py predicted but not needed).

## Acceptance (one entry per criterion — red-green evidence)
- [#16 AC0 — use_transformed=false joins raw instance table, unchanged] ->
  test: tests/test_builtins.py :: test_execute_join_raw_unchanged_when_use_transformed_false |
  red: n/a — guards prior behavior, green before & after (a transformed staging table is
  present but ignored; raw tags {raw1,raw2} returned) |
  green: 1 passed (raw path joined, transformed staging ignored).
- [#16 AC1 — use_transformed=true joins transformed frame via resolve_frame] ->
  test: tests/test_builtins.py :: test_execute_join_transformed_uses_resolved_frame |
  red: `assert len(result) == 3` -> `assert 2 == 3` (raw 2-row join returned; transformed ignored) |
  green: 1 passed (transformed staging tags {XF1,XF2,XF3} joined, len 3).
- [#16 AC2 — transformed join against never-run right source materializes on demand] ->
  test: tests/test_builtins.py :: test_execute_join_transformed_materializes_never_run_right_source |
  red: `assert set(result["id"]) == {2, 4}` -> `assert {1,2,3,4} == {2,4}` (raw 4 rows; pipeline not run) |
  green: 1 passed (resolve_frame ran the right pipeline; filter kept ids {2,4}).
- [#16 AC5 — join over null/type-messy keys behaves correctly] ->
  test: tests/test_builtins.py :: test_execute_join_messy_null_keys |
  red: n/a — guards SQL null-key semantics, green before & after (NULL=NULL never matches;
  keys {a,b} paired correctly, no row corruption) | green: 1 passed.
- [#17 AC3 — right-column-fetch endpoint returns transformed cols when flag set, raw when not] ->
  test: tests/test_api_pipelines.py :: test_join_columns_endpoint_raw_vs_transformed
  (+ test_join_columns_endpoint_404_unknown_source) |
  red: `assert raw_resp.status_code == 200` -> `404 == 200` ({"detail":"Not Found"} — route absent) |
  green: 2 passed (raw omits 'derived'; transformed includes 'derived'; unknown source -> 404).
- [#18 AC4 — JoinModal still sends use_transformed and requests the transformed column set] ->
  test: src/pipeui/frontend/screen-builder.test.jsx ::
  "JoinModal — use_transformed toggle (slice 2 / #18 regression)" (3 cases) |
  red (proven by temporarily forcing use_transformed=false / loadRightColumns(false) in the
  component): 2 of 3 failed — fetchRightColumns called with ("R2", false) not (…, true) and
  onSubmit got use_transformed:false; toggle-off case stayed green |
  green: 3 passed after restoring the component (toggle-on -> fetchRightColumns("R2", true) and
  use_transformed:true submitted; toggle-off -> ("R1", false) and use_transformed:false).

## Follow-ups / debt
- Test-evidence default for this repo is **archive** (.claude/conventions.md). The
  six builtins/api guarantees (AC0–AC5/AC3) assert stable join-resolution behavior, not
  phrasing, so they are good promote candidates if regressions here prove costly; left to
  the integrator per the archive default. The frontend toggle regression is phrasing-light
  and worth keeping as a live guard against the toggle silently regressing again.
- AC0/AC5 have no red phase (they guard pre-existing raw-join behavior that is correct
  today); their value is preventing a regression as the resolve seam evolves.
- Pre-existing unrelated failure observed in the full suite:
  tests/test_worker.py::test_oom_worker_killed_by_setrlimit_returns_failed_entry fails on
  the clean base commit too (setrlimit/OOM env-dependent) — not introduced by this slice.
