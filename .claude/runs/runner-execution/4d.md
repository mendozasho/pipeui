# Slice 4d — Builder palette builtin detail drawer (#256)
Issue: #256   Branch: feature/runner-execution-4d

to-code addendum (NOT in the frozen slice ledger). Gap found in manual testing of PR #253: builtin palette cards had no detail drawer (functions/sets do). User directed: build now, fold into #253.

## Plan
- Mirror PaletteFunctionDrawer/SetDetailDrawer; reuse window.__UI__.Drawer (no new primitive, same constraint as #244).
- Render from the already-loaded /builtins payload (no fetch).
- Test command: `npx vitest run src/pipeui/frontend/screen-builder.test.jsx`
- planned_touched_files: [src/pipeui/frontend/screen-builder.jsx, src/pipeui/frontend/screen-builder.test.jsx, .claude/runs/runner-execution/4d.md]

## Actual
- Built: new `PaletteBuiltinDrawer` (read-only: display_name title, builtin_type tag, description, config_schema summary), wired `onOpenDrawer` into `PaletteBuiltinCard` (onClick, separate from onDragStart), added `selectedBuiltinId` state + drawer mount in RightPalette, exported the component.
- actual_touched_files: src/pipeui/frontend/screen-builder.jsx, src/pipeui/frontend/screen-builder.test.jsx, .claude/runs/runner-execution/4d.md
- Unanticipated files: none.

## Acceptance (red→green, screen-builder.test.jsx)
- Click opens drawer with metadata → `PaletteBuiltinDrawer (#256) :: renders the builtin's name, type, description and a config summary inside the reused Drawer` | red: `Element type is invalid ... PaletteBuiltinDrawer undefined` | green.
- Card click calls onOpenDrawer(builtin_type) → `PaletteBuiltinCard (AC9) :: calls onOpenDrawer with the builtin_type when clicked (#256)` | red→green.
- Drag still works, click != drag → `... still fires onDragStart when dragged ... (#256)` | green.
- No builtin → renders nothing → `... renders nothing when no builtin is provided` | red→green.

## Follow-ups / debt
- Tests promoted (assert stable component behavior, reuse of the shared Drawer).
