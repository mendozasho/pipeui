# Slice 6 — Docs reconciliation (design.md + CLAUDE_REFERENCE §12)
Issue: #223   Branch: feature/runner-execution-6

## Plan (written before coding)
- Approach per layer (docs only):
  - Reconcile `.claude/design.md` §"Backend Perspective - function classification"
    (lines ~252–260) and `.claude/CLAUDE_REFERENCE.md` §12 toward the frozen
    execution model captured in `.claude/CONTEXT.md` (canonical glossary) and
    `docs/adr/0001-multi-select-execution-model.md` (ADR, accepted). No new
    decisions are made — the docs are made to stop contradicting the already-frozen
    artifacts.
  - design.md: remove `pd.DataFrame` from the `multi_select_eligible` enumeration
    and annotate it "not eligible — full table always passed, see §12"; replace the
    single-param-only example with the multi-param argument-bundle model
    (positional pairing, static-param broadcast, equal-length-among-varying rule),
    using the canonical terms `scalar run`, `argument bundle`, `static param`.
  - CLAUDE_REFERENCE §12: replace the "Multi-select loop" single-param bullet with
    the argument-bundle model so it agrees with design.md and CONTEXT.md; §12
    already correctly excludes pd.DataFrame from multi-bind, so the pd.DataFrame
    sub-bullets are kept/aligned, not contradicted.
- Utilities/patterns reused (file + symbol):
  - Canonical term definitions copied verbatim-in-spirit from `.claude/CONTEXT.md`
    (`scalar run`, `argument bundle`, `static param`, `multi_select_eligible`).
  - ADR cross-reference `docs/adr/0001-multi-select-execution-model.md` Consequence
    "pd.DataFrame params never drive expansion".
  - Doc test is a pure file-reading pytest module — no `pipeui` import needed, no
    DuckDB, marked `@pytest.mark.unit`.
- Test command (for red-green cycles):
  `PYTHONPATH=src "<REPO_ROOT>/.venv/bin/python" -m pytest tests/test_docs_runner_execution.py -x`
- planned_touched_files:
  - `.claude/design.md`
  - `.claude/CLAUDE_REFERENCE.md`
  - `tests/test_docs_runner_execution.py`
  - `.claude/runs/runner-execution/6.md`

## Actual (written before push)
- What was built (1-3 sentences): Reconciled `.claude/design.md`'s function-
  classification enumeration and `.claude/CLAUDE_REFERENCE.md` §12 (plus the §11
  granularity note that fed it) with the frozen execution model — `pd.DataFrame`
  removed from the `multi_select_eligible` stack and annotated "not eligible —
  full table always passed, see §12"; the multi-param `argument bundle` model
  (positional pairing, `static param` broadcast, equal-length-among-varying rule)
  documented in both files using the canonical terms `scalar run` / `argument
  bundle` / `static param`. Added a pure file-reading pytest module that locks all
  three as observable doc content.
- actual_touched_files:
  - `.claude/design.md`
  - `.claude/CLAUDE_REFERENCE.md`
  - `tests/test_docs_runner_execution.py`
  - `.claude/runs/runner-execution/6.md`
- Deviations from plan (and why): (1) Also aligned the §11 granularity sentence
  (CLAUDE_REFERENCE lines ~515-517) — it still carried the old single-param
  "runs once per argument … aggregated in the summary" shorthand sitting directly
  above §12; left as-is it would have contradicted the reconciled §12 and undercut
  criterion #2 ("no remaining contradiction"). Same file (`.claude/CLAUDE_REFERENCE.md`),
  already in planned set; no new file. (2) The doc test's section-slicing helper
  was anchored on the markdown header `### Backend Perspective…` rather than the
  bare section name, because an earlier cross-reference to that name (design.md
  line 145) otherwise widened the slice and produced a false red. Pure test-helper
  fix, surfaced during the red run.
- Unanticipated files (in actual but not planned or issue touched_files): none.
  (Issue `touched_files` lists `.claude/design.md`, `.claude/CLAUDE_REFERENCE.md`,
  `.claude/runs/runner-execution/6.md`; the authored test
  `tests/test_docs_runner_execution.py` is the required test file for a docs slice
  and was in `planned_touched_files`.)

## Acceptance (one entry per criterion — red-green evidence)
- #0 design.md no longer lists pd.DataFrame as multi_select_eligible (annotated
  'not eligible — full table always passed, see §12') ->
  test: `tests/test_docs_runner_execution.py :: test_design_does_not_list_pd_dataframe_as_multi_select_eligible`
  | red: `AssertionError: design.md still enumerates pd.DataFrame in the multi_select_eligible argument stack` (section still said "stack of column arguments (`pd.Series`, `column_backed`, or `pd.DataFrame`)")
  | green: `PASSED` — pd.DataFrame removed from the stack; annotation "not eligible", "full table always passed", and "§12" all present.
- #1 design.md documents the multi-param argument-bundle positional-pairing model
  with static-param broadcast and the equal-length rule, using canonical terms ->
  test: `tests/test_docs_runner_execution.py :: test_design_documents_argument_bundle_model_with_canonical_terms`
  | red: `AssertionError: design.md classification section is missing 'argument bundle'`
  | green: `PASSED` — `argument bundle`, `static param`, `scalar run`, `position`, `broadcast`, and the equal-length rule all present in the classification section.
- #2 CLAUDE_REFERENCE §12 is consistent with design.md and CONTEXT.md (no remaining
  contradiction on multi-select / pd.DataFrame) ->
  test: `tests/test_docs_runner_execution.py :: test_reference_section12_consistent_no_contradiction`
  | red: `AssertionError: §12 does not use the canonical term 'argument bundle'` (old "runs N times (once per column)" loop framing)
  | green: `PASSED` — §12 now uses `argument bundle` + `static param`, describes positional pairing and the equal-length rule, drops the "once per column)" framing, and keeps "pd.DataFrame … excluded from multi-bind".

Final: 3 passed (`pytest tests/test_docs_runner_execution.py`).

## Follow-ups / debt
- These are **phrasing-bound** documentation assertions (they check that specific
  prose exists in design.md / CLAUDE_REFERENCE §12). Under the archive default,
  they are slice-scoped evidence — **archive** at integration, not promote. If the
  docs are re-worded later the assertions would need updating; they assert document
  content, not stable runtime behavior, so they do not belong in the permanent
  runtime suite.
